#include <stdio.h>
int main() {
    char str[100];
    char *p = str;
    int len = 0;
    gets(str);
    while(*p != '\0') {
        len++;
        p++;
    }
    printf("Length = %d\n", len);
    return 0;
}
