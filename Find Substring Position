#include <stdio.h>
#include <string.h>
int main() {
    char s[100], t[100];
    gets(s);
    gets(t);
    char *pos = strstr(s, t);
    if(pos) printf("%d\n", pos - s);
    else printf("-1\n");
    return 0;
}
